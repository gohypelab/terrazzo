{
  "name": "terrazzo-docs",
  "private": true,
  "scripts": {
    "check": "npm audit --audit-level=moderate && npm run docs:build",
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  },
  "overrides": {
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
